class Question {
  String questionText;
  bool answer;

  // Question(String question, bool answer) {
  //   this.questionText = question;
  //   this.answer = answer;
  // }
  //the above code can be replace like this
  Question(this.questionText, this.answer);
}
